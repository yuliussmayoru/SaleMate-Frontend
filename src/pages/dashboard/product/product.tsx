import ProductPage from 'features/dashboard/Product/Product';
import Layout from 'features/dashboard/Layout';

export default function Product() {
    return (
        <Layout>
            <ProductPage />
        </Layout>
    );
}
