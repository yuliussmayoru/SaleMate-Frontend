import DriverPartnerPage from '@/src/features/dashboard/DriverPartner';
import Layout from '@/src/features/dashboard/Layout';

export default function DriverPartner() {
    return (
        <Layout>
            <DriverPartnerPage />
        </Layout>
    );
}
