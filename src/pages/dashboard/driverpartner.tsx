import DriverPartnerPage from 'features/dashboard/DriverPartner';
import Layout from 'features/dashboard/Layout';

export default function DriverPartner() {
    return (
        <Layout>
            <DriverPartnerPage />
        </Layout>
    );
}
