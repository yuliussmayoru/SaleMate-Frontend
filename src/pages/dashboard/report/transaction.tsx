import TransactionPage from 'features/dashboard/report/transaction';
import Layout from 'features/dashboard/Layout';

export default function Transaction() {
    return (
        <Layout>
            <TransactionPage />
        </Layout>
    );
}
