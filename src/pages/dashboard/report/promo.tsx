import PromoPage from 'features/dashboard/report/promo';
import Layout from 'features/dashboard/Layout';

export default function Promo() {
    return (
        <Layout>
            <PromoPage />
        </Layout>
    );
}
